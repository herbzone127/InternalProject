<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ideaForge.Pages.DashboardPages"
             xmlns:mah="http://metro.mahapps.com/winfx/xaml/controls" x:Class="ideaForge.Pages.DashboardPages.Requests"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <mah:MetroTabControl>
            <TabItem Header="Today's Requests" Background="#A9ABB1" Foreground="White">
                <Grid Background="#FFF">
                    <DataGrid ItemsSource="{Binding Path=Albums}" 
          AutoGenerateColumns="False">
                        <DataGrid.Resources>
                            <Style TargetType="{x:Type DataGridColumnHeader}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </DataGrid.Resources>
                        <DataGrid.Columns>
                          
                            <DataGridTextColumn Header="Booking Id"/>
                            <DataGridTextColumn Header="UserName &amp; Contact"/>
                            <DataGridTextColumn Header="IF Dock"/>
                            <DataGridTextColumn Header="Date &amp; Time Slot"/>
                            <DataGridTextColumn Header="Status"/>
                            <DataGridTextColumn Header="Action"/>
                          
                        </DataGrid.Columns>
                    </DataGrid>
                </Grid>
            </TabItem>
            <TabItem Header="All Requests"  Background="#A9ABB1" Foreground="White">
                <Grid Background="#FFF"/>
            </TabItem>
        </mah:MetroTabControl>

    </Grid>
</UserControl>
